# 中华诗词地图 · 唐风宋韵

一个面向青少年儿童的中国诗词地图网页。青绿山水长卷上标注唐诗宋词的诞生地标，
点击地标，右侧纸卡展开竖排诗文、标签、诗意简析与完整注释。

**全部离线**：地图、诗词、作者、标签均为本地数据，无任何外部请求（无瓦片、无 CDN、无字体外链、无图片文件）。

---

## 一、快速开始

```bash
node server.js          # 打开 http://127.0.0.1:5179/
```
所有资源都是本地文件，直接双击 `index.html` 也能运行（`file://` 下同样有效）。

---

## 二、目录结构

```
poemMap/
├── index.html              页面骨架（题名·卷名·搜索筛选·篇目·地图·详情卡·图例·动效开关）
├── server.js               零依赖静态服务器（仅本地预览用）
├── css/style.css           全部样式
├── js/
│   ├── app.js              主逻辑（地标聚合、绘制、筛选、详情卡、动效挂载）
│   ├── terrain.js          青绿山水地形生成器（山系·白雾·点缀，全程序绘制）
│   ├── atmosphere.js       Three.js 云气层（雾霭·流云·落英·墨点·光尘·墨晕）
│   ├── motion.js           GSAP 动效层（开场序列·地标入场·诗卡展开·涟漪）
│   ├── mapimage.js         可选：用「地理配准准确」的底图整图替换
│   └── eave.js             金黄屋檐 SVG 生成器（当前设计未启用，见第八节）
├── data/
│   ├── authors.js          作者资料库（生卒年 + 简介）
│   ├── poems.tang.js       唐诗数据
│   ├── poems.song.js       宋词数据（含苏轼 37 首）
│   ├── tags.js             每首诗的主题标签（行旅/山水/登临…）
│   ├── china.geo.js        中国省界离线数据（367 KB）
│   └── geo-extras.js       长江/黄河/珠江/淮河/京杭大运河/长城 示意线
├── assets/                 设计素材（本地图片，全部离线）
│   ├── map-painting.png    素材中的「青绿中国地图」（留作参考底图）
│   ├── marker-*.png        地标：亭（唐）· 塔（宋）· 牌楼（多处题咏）
│   ├── pin.png             选中地标的大头针
│   ├── compass.png         罗盘
│   ├── mist-band.png 等    点景：雾带（唯一上地图的具象素材）
│   ├── mountains-mist.png  诗卡底部的一抹淡山水
│   └── crane / pines / waves / dialog / seal-*.png   备用（见第八节）
├── vendor/                 全部本地内置，无任何外链
│   ├── leaflet.*           Leaflet 1.9.4（矢量地图）
│   ├── three.min.js        Three.js r149（WebGL 云气层，UMD 版）
│   └── gsap.min.js         GSAP 3.12（动效时间线）
└── tools/                  构建与调试工具（站运行时不依赖）
    ├── build-geo.js        由原始行政区划 JSON 生成 data/china.geo.js
    ├── china-raw.json      原始行政区划数据（构建输入）
    ├── prepare-assets.js   整理素材：改英文名、自动裁掉地图画稿里的游离山石
    ├── calibrate-map.js    解算画稿的地理 bounds（陆/海取样 + 粗到细网格搜索）
    ├── check-landmarks.js  校验诗词地标是否落在画中陆地
    ├── palette-from-painting.js  从画稿取「区域色」与「山峦色阶」
    ├── cdp.js              无头 Chrome 验证器（取数 / 截图 / 模拟移动端与减弱动效）
    ├── qa-motion.js        动效与交互自检脚本（配合 cdp.js 使用）
    └── pixdump.js          截图取色工具
```

---

## 三、如何新增一首诗词

**第一步**：作者登记在 `data/authors.js`。

**第二步**：在 `data/poems.tang.js` / `poems.song.js` 数组末尾追加：

```js
{
  id: "liangzhou-ci-wangzhihuan",   // 全局唯一
  title: "凉州词",
  dynasty: "唐",                     // 唐 | 宋
  author: "王之涣",                   // 必须在 authors.js 中存在
  form: "诗",                        // 诗 | 词
  place: {
    name: "凉州",                    // 地标名，显示在地图上
    region: "甘肃 · 武威",            // 今属地区
    lat: 37.93, lng: 102.64,        // 纬度 / 经度（WGS-84）
    origin: "盛唐边塞诗代表作……"      // 写作背景（可留空）
  },
  lines: ["黄河远上白云间，", "一片孤城万仞山。", "羌笛何须怨杨柳，", "春风不度玉门关。"],
  prologue: "",                      // 词的小序（可省略）
  tr: "黄河好像从远方的白云间奔流而来……",   // 白话译文（卡片「诗意简析」用）
  notes: [["羌笛", "古代西部羌族吹奏的管乐器。"]],
  appr: "以壮阔苍凉的边塞图景写戍人哀怨……"
}
```

**第三步**（可选）：在 `data/tags.js` 里给这首诗加主题标签：

```js
"liangzhou-ci-wangzhihuan": ["边塞", "思乡"],
```

新增后刷新页面即可，无需构建。

### 地标自动合并规则
两首诗坐标相差小于 0.15°（纬度）与 0.2°（经度）时视为同一地标，地图上只出现一枚小筑，
右上角显示数量徽章；点击弹出浮卡列出该处全部诗词。想让两首诗分开显示，把坐标写准即可（阈值约 15 公里）。

---

## 四、界面与设计对应

| 设计稿元素 | 实现位置 |
| --- | --- |
| 左上朱印「诗」+ 标题 + 竖排小联 | `#brand`、`.brand-couplet` |
| 顶部中央「大河长卷／诗意中国」 | `#topnote` |
| 右上搜索框（末端山形标记） | `#toolbar .search`、`.search-mark` |
| 深色筛选条（全部/唐诗/宋词 ｜ 全部/诗/词） | `.chipbar`、`.chip` |
| 左侧「125 首诗词 · 64 处地标」+ 随机一首 | `#sidebar .side-head` |
| 侧栏页签 全部／唐诗／宋词（红色下划线） | `.tabs`、`.tab` |
| 篇目行：篇名 / 唐·李白 / 📍地名 / 箭头 | `.item` |
| 底部胶囊图例 | `#legend` |
| 右侧竖排「诗在山河间／山河皆成诗」 | `#sidePoem` |
| 右下「山水有清音／诗心自长存」 | `#motto` |
| 罗盘 | `#compass` |
| 详情卡：朱印标题、竖排诗文、地名、标签、诗意简析、查看完整注释 | `#detail` |

### 交互要点
- **地标**：小筑（朱红屋顶 + 米白墙体）。选中后换成**朱红指针**并在下方显示地名签（如「夔州」）。
- **地图标语**：地名签按题咏数量优先排布，压盖时先退为单行地名，再退为不显示；默认视野约 15 个，放大后增多。
- **详情卡**：短诗竖排（右起，与古籍一致）；超过 20 句或单句过长（如《琵琶行》88 句）自动改横排，避免卡片过长。
- **查看完整注释**：展开字词注释、赏析、写作背景、作者。
- 顶栏 chip 与侧栏 tab 共用同一筛选状态，双向同步。

---

## 五、地图是怎么画的

不加载任何瓦片或图片，全部由矢量程序绘制，分七个图层（层次即画法）：

```
省区底色(400) → 山体(410) → 水系(418) → 白雾(424) → 国境(430) → 点缀(436) → 地名(470)
```

- **省区底色**：按东北/华北/华东/华中/华南/西南/西北分区设色。颜色**取自设计画稿**——
  `tools/palette-from-painting.js` 逐省在画上网格取样、取中位色，程序里只是略作提亮，
  于是平原浅、山峦深，青绿的调子与画稿一致。
- **山体**（`js/terrain.js`）：22 条山系写成「脊线 + 宽度」，然后**由多个山组拼接**——沿脊线
  每约 0.9° 摆一组「2×3 座峰」，后排偏北色浅（远山）、前排偏南色深（近山），底下压一条
  用山阴色画的山根，脊线再描一道淡墨。山色同样取自画稿的亮度分位（山阴 #78a08e / 山腰 #92c3b8）。
- **白雾**：谷地铺白色多边形，整个图层做高斯模糊，山与山之间留白。
- **点缀**：只保留**边缘柔软的雾带**（`assets/mist-band.png`）。仙鹤、松林、海涛那类具象素材
  在地图尺度下会像「贴纸」，因此不上地图，改作界面点缀——理由见第八节。
- **地名**：32 个省名（放大后浮现）+ 河流名 + 诗词地标地名签。

### 素材里的那张「青绿中国地图」怎么处理？
它**不作为底图**。用 `tools/calibrate-map.js` 做过配准：以一批已知陆/海坐标做粗到细搜索，
`tools/check-landmarks.js` 再检验 67 处诗词地标的落点。结论是——画稿的经纬比例与真实中国不一致
（东海岸外凸、西南与北端被简化），整图替换会让地标对不上位置。所以改为**只参考它的设色**
（区域色 + 山峦色阶），既得画稿气韵，又保住地理准确与任意缩放清晰。

```js
// 若日后拿到地理配准准确的底图，js/mapimage.js 里填上即可整图替换
window.MAP_OVERLAY = { url: "assets/xxx.png", bounds: [[南纬, 西经], [北纬, 东经]] };
```

---

## 六、动效与三维

在「青绿山水」之上又叠了两层，互不依赖，任何一层加载失败都不影响阅读：

```
Leaflet 矢量地图 → ①Three.js 云气层(#atmosphere, z300) → 纸纹晕染(z460) → 界面
                                                                        ②GSAP 动效层
```

### ① Three.js 云气层（`js/atmosphere.js`）
- 一整幅透明的 WebGL 画布浮在地图上，用**真三维透视相机**摆放精灵：
  远山雾霭（z≈-300）→ 流云（z≈-100）→ 墨点 → 落英（z≈0~150）→ 光尘（加色混合）。
- **视差**：鼠标移动会让相机轻微偏移，远近层自然错开；地图平移时云气也会「跟不上」，
  形成景深——这是用 Three.js 而非平面动画的主要理由。
- **纹理全部由 Canvas 现场生成**（`feTurbulence` 式噪声团、贝塞尔花瓣、环形墨晕），
  因此不引入任何图片文件，离线约束不变。
- 选中地标时，在该位置（经投影换算到世界坐标）散开一圈**墨晕**，1.1 秒扩散淡出。
- 性能：桌面约 78 枚精灵、移动端自动降到 0.55 倍；像素比上限 1.6；页面切到后台自动暂停；
  WebGL 不可用时静默跳过（地图与交互照常）。

### ② GSAP 动效层（`js/motion.js`）
- **开场**：卷首墨色化开 → 朱印落定（带一圈扩散的光晕）→ 题名逐字从模糊中浮出 →
  竖排小联、工具条、侧栏、篇目、图例、罗盘依次到位 → 64 处地标自西向东次第点出 →
  统计数字滚动到 125 / 64。
- **诗卡**：卡片以 `perspective(1400px)` 三维展开，诗句逐列落下（左右交错），
  随后地名、标签、诗意简析、按钮依次浮现；入场结束后才挂上「门帘轻摆」的持续微动。
- **交互**：选中地标荡开涟漪、切换筛选时地标与篇目重新依次入场、地标浮卡缩放浮现。
- **从篇目或「随机一首」进入时，地图会缓缓飞向该地标**（`flyTo`），点地图上的地标则不飞。

### ③ 动效开关
右上筛选条最右侧的圆形按钮即**动效开关**（太阳形图标），状态记入 `localStorage`。
首次访问时：若系统开启了「减弱动效」（`prefers-reduced-motion: reduce`），默认关闭。
关闭后云气层隐藏、所有动画停止，页面回到纯静态呈现。

### 安全约定
开场隐藏态写在 CSS（`html.motion-prep`），JS 只负责「动到明处」。任何异常、关闭动效、
或 3.2 秒兜底超时都会立刻移除该 class——**内容不可能卡在不可见状态**（自检脚本已覆盖此路径）。

---

## 七、当前数据规模

| 项目 | 数量 |
| --- | --- |
| 诗词 | 152 首（唐 66 · 宋 86） |
| 体裁 | 诗 91 · 词 61 |
| 作者 | 54 位（含生卒年与简介） |
| 苏轼 | 37 首（词 34 · 诗 3） |
| 地标 | 67 处（含惠州、儋州等苏轼贬所） |
| 标签 | 152 首全部标注（2–3 个主题词） |

---

## 八、素材怎么用（为什么有些只当点缀）

素材分两类，用法完全不同：

| 素材 | 去处 | 理由 |
| --- | --- | --- |
| 亭 · 塔 · 牌楼 | **地标图标** | 有明确语义：唐用亭、宋用塔、多处题咏用牌楼；缩到 27px 仍认得出 |
| 定位大头针 | **选中地标** | 与「当前在看哪一首」直接对应 |
| 罗盘 · 金色祥云 | 罗盘控件 · 诗卡两角 | 位置固定、尺寸可控，不会与地图信息打架 |
| 青绿中国地图 | **只取色，不做底图** | 经纬比例与真实中国不一致（见第五节） |
| 雾带（边缘柔软） | **唯一上地图的具象素材** | 在画理里它就是「留白」，叠在山脉上只会增加空气感 |
| 山水 · 松林 · 仙鹤 · 海涛 · 对话框 · 圆形印章 | 备用，暂不上地图 | 在地图尺度下它们会成为**贴纸**：具象轮廓 + 固定像素尺寸，与矢量地貌的比例尺永远对不齐 |

已经落地的两处「气韵」用法：诗卡底部的一抹淡山水（8% 不透明度）、卡角金色祥云。
其余素材保留在 `assets/`，需要时再取——例如：
- 打开某首诗时，用「仙鹤」从地标处飞入（配合 `MOTION` 时间线即可）；
- 首页首次进入时以「卷轴」展开代替现在的墨色开场；
- 把「对话框-金色描边」用 `border-image` 做成地标浮卡的外框。

> 判断标准很简单：**素材的语义要对应界面上的一个对象**（地标、罗盘、卡片），
> 而不是散落在地图上当花纹。散落的装饰越多，地图越像贴纸册，越不像山水长卷。

---

## 九、说明与后续

- **`js/eave.js` 未启用**：早前版本用过「中央红墙 + 金黄屋檐 + 诗词门帘」的详情呈现，
  现按设计稿改为右侧纸卡，该文件保留备用，未在 `index.html` 中加载。
- **可扩展**：拼音注音（在 `lines` 旁加 `pinyin` 数组）、Web Speech 朗读、更多朝代
  （`dynasty` 为自由字段，需同步增加筛选按钮）。
- **换更精细的省界**：替换 `tools/china-raw.json` 后运行 `node tools/build-geo.js`。
- **再补诗词**：追加到 `data/poems.song.js` 末尾（注意上一条 `}` 后要有逗号），
  再到 `data/tags.js` 补标签即可。

### 验证方式（无需外部浏览器）
```bash
node server.js                                   # 起服务
node tools/cdp.js http://127.0.0.1:5179/ a.png 7000        # 截图
node tools/cdp.js http://127.0.0.1:5179/ --eval "@tools/qa-motion.js" 8000   # 动效自检
$env:CDP_REDUCED=1; node tools/cdp.js ...        # 模拟「减弱动效」
$env:CDP_MOBILE=1;  node tools/cdp.js ...        # 模拟移动端
$env:CDP_VERBOSE=1; node tools/cdp.js ...        # 打印网络请求，核对零外链
```

