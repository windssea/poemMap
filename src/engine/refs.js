/* ============================================================
   共享引用
   ----------------------------------------------------------
   React 组件把自己需要被「命令式定位」的元素登记到这里，
   anchor.js 才能在不依赖 React 的前提下量尺寸、算坐标。
   （卡片与浮层的位置跟地图投影、视口、篇目栏宽度都有关，
     写成命令式比在 React 里绕一圈更直接、也更好调。）
   ============================================================ */

export const els = {
  card: null,       // #card 元素
  poemList: null,   // #poemList 元素
  sidebar: null,    // #sidebar 元素
  hover: null,      // #placeHover 地标悬停卡
};

export const mapRef = { map: null };
